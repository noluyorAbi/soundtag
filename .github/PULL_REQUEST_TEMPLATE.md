## What changed

<!-- One paragraph. What was wrong, what this does about it. -->

## How it was checked

- [ ] `npm run typecheck && npm run lint && npm run lint:prose && npm test` passes
- [ ] Geometry change: the closure test still holds, and a real slice was opened in a slicer
- [ ] Claim change: any number in the docs is one that was measured, not one that was estimated
