node_modules
.next
# Any .env, because cp .env.example .env is the natural setup step and the
# first line of that template is a client secret.
.env*
!.env.example
.data
*.stl
*.3mf
.DS_Store
tsconfig.tsbuildinfo
.vercel

# local agent config
.claude/

# bundled CLI and library output
dist

# npm pack output
*.tgz

# private planning layer: the standards brief and the launch plan
planning/

# launch skill run state
.launch

# rendered video, the distribution files in assets/ are what ships
video/out/
video/node_modules/
